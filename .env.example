# ============================================================
# CMS v1.2.0 - Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ============================================================

# ── FTP Server ───────────────────────────────────────────────
FTP_HOST=localhost
FTP_USER=anonymous
FTP_PASS=
FTP_BASE_PATH=/

# ── AI Image Generation (Nano Banana API) ────────────────────
# Required for AI image features (t2i/i2i/m2i modes)
BANANA_API_KEY=
BANANA_API_URL=https://api.nanobanana.com/v1

# ── CSRF Secret ──────────────────────────────────────────────
# Used to sign CSRF tokens (HMAC). Auto-generated if not set.
# Set a fixed value in production so tokens survive redeployments.
CSRF_SECRET=

# ── CORS ─────────────────────────────────────────────────────
# Comma-separated list of allowed origins
# Development: http://localhost:5173
# Production:  https://your-project.vercel.app
CORS_ORIGINS=http://localhost:5173
